This directory contains the sql queries to recreate the database.

Run order is based on the numerical prefix in ascending order (1, 2, 3, ...).

Run the table queries first, and then the policy queries in the '/policies' subdirectory.
